<% content_for :title, "Edit Submission" %>

<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl mb-5">Edit Submission</h1>

  <%= render "form", volunteer_submission: @submission %>

  <%= link_to "Back to submission", admin_volunteer_submission_path(@submission), class: "ml-auto rounded-md py-3 px-3.5 inline-block font-medium text-gray-500 hover:text-gray-800" %>
</div>