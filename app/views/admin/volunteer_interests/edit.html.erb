<% content_for :page_title, "Edit: #{@interest.name}" %>

<div class="mb-6">
  <%= link_to "← Back to categories", admin_volunteer_interests_path, class: "text-sm text-ivory-500 hover:text-ivory-700" %>
</div>

<div class="max-w-xl">
  <%= render "form", volunteer_interest: @interest %>
</div>
