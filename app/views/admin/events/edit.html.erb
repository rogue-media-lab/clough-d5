<% content_for :page_title, "Edit: #{@event.title}" %>

<div class="mb-6">
  <%= link_to "← Back to event", admin_event_path(@event), class: "text-sm text-ivory-500 hover:text-ivory-700" %>
</div>

<h1 class="text-2xl font-bold text-ivory-900 mb-6">Edit Event</h1>

<%= render "form", event: @event %>
